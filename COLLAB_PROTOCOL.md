# COLLAB_PROTOCOL.md — codem-skills 多机协作更新协议（2026-09-22 用户定稿）

> 适用：任何机器上的任何 agent 更新本仓库（含 report-kit-builder 等所有技能）。
> 核心原则：**main 只进合并的改动**。任何人（含维护机和创建者本人）一律走「分支 → PR → 评审 → 用户授权合并」。

## 零、更新资格红线（2026-09-22 用户定稿：无完整技能源码禁止更新）

要更新这个技能，**必须先拥有完整的技能源码**——不满足以下三条的 agent **禁止对本仓库做任何更新**（含提 PR；评审 agent 见此类 PR 一律判「建议不合」）：

1. **完整 clone**：本机 `~/.codem/skills` 是本仓库的 git clone（有 `.git`），而非 zip 拷贝、散装文件或部分目录
2. **最新状态**：更新前已 `git pull origin main`，本地基于最新 main 切分支；旧版本/落后 commit 之上做的改动一律作废重做
3. **版本硬校验通过**：核对 `report-kit-builder/VERSION` 与最新版一致，不符先 pull 再动手

## 一、改动流程（所有机器统一）

0. **小 PR 原则（业界标准）**：一个 PR 只做一件事；diff 超过 ~400 行必须拆分（sync_skill.sh 全量同步按资产组拆 PR，如 theme/report-kit/pages 各一个）
0. **自查后再提**：提 PR 前 agent 必须把 diff 从头到尾过一遍，清除调试代码、临时文件、误传文件
0. **UI 改动必附截图**：凡涉及页面/组件改动，PR 必须附改动前后截图（承接妙搭防回退复截机制，把「防丢细节」提前到合并前）
0. **Draft 预对齐**：大改动（新组件/新规则方向）先开 Draft PR 或文字方案给用户看方向，用户点头再做完——「先对齐再动手」红线延伸到协作流程

### 提交

1. **同步**：`git -C ~/.codem/skills pull origin main`
2. **切分支**：`git checkout -b feature/<简短描述>`（修 bug 用 `fix/<描述>`；从 main 切出）
3. **改动 + 提交**：Conventional Commits 格式（`feat:`/`fix:`/`docs:` 前缀 + 一句话说明）
4. **推分支 + 建 PR**：
   ```bash
   git push -u origin feature/<描述>
   gh pr create --base main --head feature/<描述> --title "<type>: <说明>" --body-file <按 PR 模板填写>
   ```
   若机器未配 gh 凭证：先 `gh auth login`（vellan-tcn 账号）一次，永久有效。

## 二、PR 模板（.github/PULL_REQUEST_TEMPLATE.md，创建 PR 时必填）

改了什么 / 为什么改（触发来源：哪个项目的什么问题）/ 影响范围（哪些技能、哪些复刻规则）/ 如何验证（截图/diff 自检/清单核对结果）/ 风险与回滚方式。

## 三、AI 辅助判断（PR 评审协议，任意 agent 执行）

PR 建立后（或用户把 PR 链接发进任意对话后），对话中的 agent **必须**先做评审再谈合并；**拉到 PR 链接的当轮必须出评审意见，不许挂起**：

1. `gh pr diff <PR编号>` 拉取改动
2. 按五维评审：
   - **真源性**：改动是否基于真源代码/实际复刻复盘（引用必查证，不许凭印象）
   - **一致性**：与 SKILL.md 现有红线冲突吗？版本号/changelog/FEATURE_MATRIX 同步更新了吗？
   - **回退风险**：删/改了已有规则吗？删的理由充分吗（只减不许不加：新规则允许加，删旧规则必须给证据）？
   - **完整性**：跨文件联动改齐了吗（SKILL.md/VERSION/矩阵/脚本）？
   - **可复用性**：是项目专属（不该进 skill）还是通用（该进）？
3. 输出结构化评审意见（建议合 / 建议不合 + 理由 / 需用户决策的点），可直接 `gh pr comment` 贴到 PR
4. **合并判据（2026-09-22 用户定稿）**：
   - **内容相同 → 不合并，保持原样**：PR 改动与 main 现有内容相同或等价（重复实现、换汤不换药、无实质差异的重写），一律判「建议不合」，main 保持原有内容不动——不为「换个人写的同款」产生合并噪音
   - **仅当实质区别才合**：改动须满足其一——更优方案（效果/性能/可维护性确实更好）、修 bug、补盲区（main 没有的能力）；评审时须在意见里明确指出「与 main 现有内容的实质区别是什么」
   - 判同方法：diff 对照 main 对应文件，等价改写（仅格式/命名/措辞不同）视同相同
5. **明确禁止**：评审未做就执行合并；agent 自行决定合并（无用户授权）
6. **PR 处置三分法（2026-09-23 用户定稿，源自 PR#7 教训：「不合就不要提示合并」）**：agent 对改动有明确技术结论时按结论处置，**不许把「确定不合」的选项抛给用户、制造虚假决策负担**：
   - ✅ **可合**（过门禁、无回退风险、符合判据）→ 提 PR，等用户授权合并
   - ❌ **确定不合**（回退性变更/违反合并判据/已知缺陷，理由是硬技术事实而非偏好）→ **不开 PR；已开的当场自关并 comment 留档关闭原因**，汇报只说结论与原因，**不提供合并选项**
   - 🤔 **真正两可**（业务取舍/偏好权衡，agent 无明确结论）→ 才交用户二选一
   - 注：「skill 基准只从麻辣真源单向流出，其他项目禁止反向 sync pages/基准资产回灌」即属 ❌ 类硬结论
7. **回灌时机/攒批规则（2026-09-23 用户定稿，源自 PR#9/#10 教训：「项目没开发完，每次提交太碎片化」）**：进行中项目**一律不产生回灌 PR**——包括资产回灌、脚本类通用改进、适配补丁，全部攒批在项目本地/分支上。**闭环判定权 100% 归用户单方：仅当用户亲自说「已经确认闭环了」等明确闭环表述时项目才算闭环**；agent 不做任何闭环判断、不参与复核、不得自行推断「项目差不多完成了」，agent 侧的复核/复述均不构成闭环依据。**只有用户确认完全闭环后，agent 才提示用户「是否把更新的技能内容/源码上传给原作者（维护机）合并」**，经用户同意后再统一回流（一次性提 PR，可按资产/脚本分 2~3 个小 PR）。仅用户明确点名「现在就修 skill」的紧急缺陷例外。目的：开发期频繁小 PR 反复打扰用户，必须攒批。原流程中的分级评审顺延为第 8 条。
8. **分级评审**：
   - Tier 1（错字/文档格式）→ 快速通道：AI 评审通过即建议合，用户口头合并
   - Tier 2（规则/组件/脚本）→ 完整五维评审
   - Tier 3（架构级：SKILL.md 红线区改动、删既有规则）→ 必须用户明确逐条确认后才能合
9. **分歧升级路径**：agent 评审意见与用户/其他 agent 冲突时 → 用户裁决，裁决结论 comment 留档到 PR（防两个 agent 各执一词死循环）
10. **删除即证据**：任何删既有规则/文件的 PR，必须附「为什么删 + 删了影响什么」的证据链（承接「只减不加」原则）
11. **agent 行为硬限**：每 PR 评论数上限 5 条（防 comment 风暴）；agent 永不许自动合并（与第 5 条重申）

## 四、用户判断合并（分布式授权）

- **授权方式**：用户在**任意一台机器的任意 agent 对话**里说「合并 PR #N」（或语义等价的话），该对话的 agent 即执行：
  ```bash
  gh pr merge <PR编号> --squash --delete-branch
  ```
- 合并后本地切回 main 并 pull；若其他机器有本地残留分支，pull 时清理即可
- **squash 合并**：一个 PR 压成 main 上一个 commit，历史干净、回滚粒度=整个 PR
- **同文件互斥（多机并发）**：改同一个文件的 PR，后到的必须先 rebase 到最新 main 再评审（防止覆盖）；评审时发现与已合 PR 冲突的，判「需 rebase 后重评」

## 五、main 分支保护（GitHub 侧，已配置）

- 禁止直接 push、禁止强推、禁止删除 main
- 必须走 PR；**required approvals = 0**（GitHub 不允许同账号 approve 自己的 PR，故 AI 评审意见以 PR comment 形式留档，**用户的合并授权就是唯一的合并开关**）
- **enforce_admins：true**——维护机管理员身份也不例外（全员走 PR，用户 2026-09-22 拍板）
- 仓库已转 **public**（2026-09-22 用户拍板：全量脱敏后公开，任何机器 clone/pull 零凭证）；**脱敏门禁**：sync_skill.sh 提交前自动扫描敏感词（客户名/内部域名/镜像仓库名/旧基线值），命中即中止并提示替换，严禁客户信息进入公开仓库

## 六、业务源码前置要求（2026-09-22 用户定稿：没有源码不行）

复刻差异化阶段凡涉及**对方项目的业务 UI / 交互**，必须先拿到**真实源码**，没有源码不许动手：

1. **必须有**：页面结构、组件代码、交互逻辑（路由跳转/状态管理/事件处理）——三类缺一不可
2. **源码获取途径**（按优先级）：对方项目源码仓库 → 妙搭应用 `lark-cli apps +init` 直接拉源码 → 对方提供的代码文件
3. **明令禁止**：凭截图、口头描述、记忆印象「想象重写」业务 UI（历史教训：凭理解重写=字号/加粗/交互细节大面积漂移）
4. **拿不到时的降级**：只交付 skill 基准骨架 + 空态，明确告知用户「缺业务源码，以下为基准占位」，并列出待补素材清单（页面清单+交互清单）索取；用户补源码后再差异化

## 七、差异化回流闭环（2026-09-22 用户定稿）

任何项目用本仓库技能做差异化修改并触发自我迭代（skill 本地更新）后，该项目对话中的 agent **必须主动提示用户**：「是否把这些差异化更新提交到 GitHub 分支？」

1. **用户选择提交** → 按「一、改动流程」完整走：切分支 → 脱敏门禁（业务专属内容：文案/数据/工况规则/选源参数一律剔除）→ commit 更新内容和源码 → push 分支 → 开 PR 请求评审
2. **用户选择不提交** → 改动只留本地，**不擅自推送**；下次自我迭代时可再提示
3. **双重禁止**：不许跳过提示；不许未经用户选择就推送分支
4. 评审按「三、AI 辅助判断」执行，合并按「四、用户判断合并」执行——差异化回流同样过全流程，不开绿色通道

## 八、版本纪律（不变）

合并后各机器开工前仍执行 SKILL.md 的「版本硬校验」：`git pull` + 核对 `report-kit-builder/VERSION`，旧版 agent 不许动手复刻。
